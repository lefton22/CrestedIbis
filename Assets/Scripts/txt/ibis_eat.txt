tree("Root")
//或许缺不同季节食物的分配，因该归于卡牌发放权重
    fallback
	        
				sequence
			 
									checkHasFood() //没食物，则失败
									Wait(120)

									Next()

			sequence              

						   seekFood()
						   Wait(10) 

						  // checkItemsOnLand() 
						  // Wait(10)

						   eat()
						   Wait(10)

						   Next()
